<?xml version="1.0" encoding="utf-8"?>
<!-- Greetings Card — MaterialCardView with time-based greeting and decorative illustration -->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="10dp"
    android:layout_marginEnd="10dp"
    android:layout_marginTop="3dp"
    android:layout_marginBottom="3dp"
    app:cardCornerRadius="10dp"
    app:cardElevation="2dp"
    app:cardUseCompatPadding="false">

    <RelativeLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/Shape_Greeting_Bg"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        android:padding="9dp">

        <!-- Decorative illustration on the right -->
        <com.makeramen.roundedimageview.RoundedImageView
            android:id="@+id/Image"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_marginTop="2dp"
            android:layout_marginEnd="2dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_post_desert"
            android:alpha="0.90"
            app:riv_corner_radius="8dp" />

        <!-- Title: e.g. "Good Morning, John" -->
        <TextView
            android:id="@+id/HeadText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toStartOf="@id/Image"
            android:layout_alignTop="@id/Image"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="0dp"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:textStyle="bold"
            android:ellipsize="end"
            android:maxLines="1"
            android:clickable="false"
            android:focusable="false" />

        <!-- Subtitle: random greeting message (up to 2 lines) -->
        <TextView
            android:id="@+id/subText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toStartOf="@id/Image"
            android:layout_below="@id/HeadText"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="1dp"
            android:textColor="#FFFDE7"
            android:textSize="11sp"
            android:maxLines="2"
            android:ellipsize="end"
            android:clickable="false"
            android:focusable="false"
            android:lineSpacingExtra="2dp" />

    </RelativeLayout>
</androidx.cardview.widget.CardView>